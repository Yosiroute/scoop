name: chain-b
on:
  workflow_run:
    workflows: ["chain-a"]
    types: [completed]
permissions: {}
jobs:
  b:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "cascaded from chain-a; upstream event = ${{ github.event.workflow_run.event }}"
          echo "root head = ${{ github.event.workflow_run.head_repository.full_name }} @ ${{ github.event.workflow_run.head_branch }}"
